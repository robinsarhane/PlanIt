git pull origin main                //save all the files from GIT to local pc
git checkout -b nameOfBranch        //creates a parallel branch (before editing)
------ CODE  -------
git add .                           //move files to staging area
git commit -m "name of commit"      //saves changes
git checkout main                   //switches to the main branch
git pull origin main                //pulls the latest changes if someone else had modified main
git merge nameOfBranch              //merging the branch to the original

//in case of conflict, fix them and repeat starting git add .

git push origin main                //saves the changes on the main
